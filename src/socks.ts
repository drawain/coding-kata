/**
 * There is a large pile of socks that must be paired by color.
 * Write a function which can determine how many pairs of socks with matching colors
 * there are based on array of integers representing the color of each sock.
 * 
 * Example: arr = [1,2,1,1,3,2,2]
 */

export default class Example { }